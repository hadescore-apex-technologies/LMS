[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py migrate"]

[start]
cmd = "gunicorn apps.core.wsgi:application --bind 0.0.0.0:$PORT"
